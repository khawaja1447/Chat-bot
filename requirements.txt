# sentence-transformers pulls PyTorch. The CPU wheel index keeps that to ~200 MB
# instead of the ~2 GB CUDA build; nothing here uses a GPU.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit==1.45.1
langchain==0.3.25
langchain-community==0.3.24
langchain-huggingface==0.3.1
faiss-cpu==1.12.0
rank-bm25==0.2.2
sentence-transformers==5.5.1
pymupdf==1.25.5
groq==1.2.0
python-dotenv==1.1.0
PyYAML==6.0.3
